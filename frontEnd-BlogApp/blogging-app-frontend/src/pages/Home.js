import Base from "../components/Base";

const Home = () => {
    return (
    
        <div>
            <h1>This is home page</h1>
            <p>Welcome to home page</p>
        </div>

    );
};

export default Home;